type Routes = {
  HOME: string;
  REGISTRATION: string;
  BASKET: string;
  AUTHORIZATION: string;
};

export const ROUTES: Routes = {
  HOME: '/',
  REGISTRATION: '/registration',
  BASKET: '/basket',
  AUTHORIZATION: '/authorization',
};
