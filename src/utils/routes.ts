type Routes = {
  HOME: string;
  REGISTRATION: string;
  AUTHORIZATION: string;
};

export const ROUTES: Routes = {
  HOME: '/',
  REGISTRATION: '/registration',
  AUTHORIZATION: '/authorization',
};
